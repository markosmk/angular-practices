import { UppercustomPipe } from './uppercustom.pipe';

describe('UppercustomPipe', () => {
  it('create an instance', () => {
    const pipe = new UppercustomPipe();
    expect(pipe).toBeTruthy();
  });
});
